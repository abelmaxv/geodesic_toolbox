# Dependencies of geodesic_toolbox, pinned to the versions the benchmarks were
# validated against (python 3.12.2). Verified by installing into a clean venv.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -e .
#
# torch below is a macOS (CPU/MPS) build; on CUDA install the matching wheel
# instead: pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cu124

# ── required to import geodesic_toolbox (all imported at module scope in src/) ─
torch==2.12.1
numpy==1.26.4
scipy==1.15.2
scikit-learn==1.6.1
einops==0.8.2
kmedoids==0.5.5
torchdiffeq==0.2.5
networkx==3.4.2
tqdm==4.67.1

# ── required to run the benchmarks ───────────────────────────────────────────
# hamiltorch (benchmark_utils.NUTS) is not on PyPI; it must come from git.
hamiltorch @ git+https://github.com/AdamCobb/hamiltorch.git@19b627b2aabc77c1b4b78db0f860372eb1bf9778
pandas==2.2.2             # benchmarks/blr_target.py

# ── plots and notebooks ──────────────────────────────────────────────────────
matplotlib==3.10.1
ipykernel==6.28.0

# ── extras for some notebooks (uncomment as needed) ──────────────────────────
# botorch==0.17.2           # parameter-tuning notebooks
# gpytorch==1.15.2
# torchvision==0.25.0
# pyro-ppl @ git+https://github.com/pyro-ppl/pyro.git
