# Direct dependencies of geodesic_toolbox, pinned to the versions in the
# `geodesic_toolbox_bis` conda env (Python 3.12.12).
#
# NOTE: that env is conda-managed. For an exact clone prefer conda (see below);
# this pip file is the portable fallback for a plain venv on the remote:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pip install -e .            # install the geodesic_toolbox package itself
#
# torch/torchvision below are macOS (CPU/MPS) builds. On a CUDA GPU machine,
# install the matching CUDA wheels INSTEAD, e.g.:
#   pip install torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu124

# ── core (needed by src/, benchmarks/, animations/) ──────────────────────────
torch==2.10.0
numpy==1.26.4
scipy==1.15.2
matplotlib==3.10.8
pandas==3.0.0
scikit-learn==1.6.1
tqdm==4.67.3
hamiltorch==0.4.1
ipykernel==7.2.0          # to run the notebooks

# ── extras used by some kept notebooks (uncomment as needed) ─────────────────
# kmedoids==0.5.3.1         # centroid-metric notebook (CentroidsCometric)
# botorch==0.17.2           # parameter-tuning notebooks (Bayesian optimization)
# gpytorch==1.15.2          #   "
# torchdiffeq==0.2.5
# torchvision==0.25.0
# pyro-ppl was installed from a git checkout (version 1.9.1+ab0491a); if a
# notebook needs it, install from source rather than PyPI:
#   pip install "pyro-ppl @ git+https://github.com/pyro-ppl/pyro.git"
